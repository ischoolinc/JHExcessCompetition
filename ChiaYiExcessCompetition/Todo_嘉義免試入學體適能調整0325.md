# 🎯 目標

新增讀取仰臥捲腹(curl)、漸速耐力跑(pacer)資料與計算，
注意不能更動原本計算邏輯（例如最高9分維持）。
修改完成記錄在：嘉義免試入學體適能調整0325.md

---

# 📌 一、修改 QueryData.cs

## 1.1 修改方法：FillStudentFitness()

### 【修改 SQL】

在 SELECT 欄位新增：

```
,curl
,curl_degree
,pacer
,pacer_degree
```

⚠️ 注意：

* 不要刪除或修改原本4項欄位
* 只做「新增欄位」

---

## 1.2 填入 StudentInfo（新增資料寫入）

在 foreach(DataRow dr ...) 區段，新增以下程式碼：

### ▶ curl_degree

```
if (dr["curl_degree"] != null)
{
    string ss = dr["curl_degree"].ToString().Trim();
    if (ss != "" && ss != "未檢測")
    {
        si.curl_degreeList.Add(ss);
    }
}
```

### ▶ pacer_degree

```
if (dr["pacer_degree"] != null)
{
    string ss = dr["pacer_degree"].ToString().Trim();
    if (ss != "" && ss != "未檢測")
    {
        si.pacer_degreeList.Add(ss);
    }
}
```

⚠️ 注意：

* 不要修改 addCount 邏輯
* 不要修改 isAddFitnessScore 判斷（仍維持 == 4）

---

# 📌 二、修改 StudentInfo.cs

## 2.1 新增屬性（放在 Fitness 區域）

```
// 仰臥捲腹
public string Curl { get; set; }

// 漸速耐力跑
public string Pacer { get; set; }
```

---

## 2.2 新增 List（用於計算）

```
public List<string> curl_degreeList = new List<string>();
public List<string> pacer_degreeList = new List<string>();
```

---

# 📌 三、修改 CalcFitnessScore()

## 3.1 保留原本4項邏輯（絕對不可修改）

原本這四段：

* sit_and_reach_degreeList
* standing_long_jump_degreeList
* sit_up_degreeList
* cardiorespiratory_degreeList

👉 完全不動

---

## 3.2 在後面新增第5、6項

### ▶ curl（仰臥捲腹）

```
foreach (string name in passStringList)
{
    if (curl_degreeList.Contains(name))
    {
        score += 3;
        break;
    }
}
```

---

### ▶ pacer（漸速耐力跑）

```
foreach (string name in passStringList)
{
    if (pacer_degreeList.Contains(name))
    {
        score += 3;
        break;
    }
}
```

---

## 3.3 保持原本上限限制（不可修改）

```
if (score > 9)
    score = 9;
```

⚠️ 這段不能改（核心規則）

---

# 📌 四、重要限制（一定要遵守）

❌ 不可修改：

* addCount == 4
* isAddFitnessScore 邏輯
* 原本4項判斷
* score 上限 9

✅ 只允許：

* 新增 curl / pacer 資料來源
* 新增 curl / pacer 計算

---

# 📌 五、預期結果

* 系統會同時判斷「6項體適能」
* 但最終分數仍維持：
  👉 最高 9 分
* curl / pacer 會影響：
  👉 是否更容易達到 9 分
* 不影響既有邏輯與報表結果

---

# 📌 六、完成後紀錄

建立文件：

```
嘉義免試入學體適能調整0325.md
```

內容需包含：

* 修改原因
* 修改檔案
* 新增欄位（curl / pacer）
* 計算邏輯說明（6項但仍9分上限）
* 測試結果